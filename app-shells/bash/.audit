The main change from upstream appears to be that we use the built-in readline bundled with bash to avoid an external readline dependency,
which in the past has been problematic. Probably wise to keep this fork.
