This ebuild contains a CVE fix (see FL-5832) but is addressed in Gentoo via a package update.
But the old unsafe version was kept around for some reason. So let's keep our curated version
and add updates until 1.19.8.1 disappears from the tree.
