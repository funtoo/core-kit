Our patches to the master.cf allow smtp service listen only on a UNIX-domain socket and make it accessible for local clients only.
We also init the alias database.

Adding latest supported versions from the 2.11, 3.0 and 3.1 releases.
Palica