Our patches to the master.cf allow smtp service listen only on a UNIX-domain
socket and make it accessible for local clients only.  We also init the alias
database, and modify the makedefs script to explicitly link against a
particular version of berkeley DB.
