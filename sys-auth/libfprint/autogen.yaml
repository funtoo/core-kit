libfprint_rule:
  packages:
    - libfprint:
        cat: sys-auth
        desc: Library to add support for consumer fingerprint readers
        homepage: https://gitlab.freedesktop.org/libfprint/libfprint
        gitlab:
          server: "gitlab.freedesktop.org"
          query: tags
