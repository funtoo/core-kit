adcli_rule:
  # TODO: use dirlisting-1 generator.  Problematically, the links in the text
  # received from gitlab.com have had all their html entities replaced by
  # their string equivalents.  The latest version is only available from the
  # gitlab.freedesktop.org releases page.  
  generator: gitlab-v4-1
  packages:
    - adcli:
        cat: app-crypt
        desc: Tool for performing actions on an Active Directory domain
        homepage: https://www.freedesktop.org/software/realmd/adcli/adcli.html
        gitlab:
          project_id: 1196
          server: "gitlab.freedesktop.org"
          user: realmd
          repo: adcli
          query: releases
