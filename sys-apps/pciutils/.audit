I forked this to add /usr/bin/lspci symlink to /usr/sbin/lspci.

Anydesk looks for "lspci" in path and it's a handy tool for non-root accounts.
Nice to have it available to users without manually specifying a path.

-drobbins ;)
