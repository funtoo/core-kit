This particular ebuild is a "fake" that installs nothing but depends on glibc *with* built-in rpcsvc. Needed for ebuilds that depend on glibc's rpc library.
Should be in 1.0-prime during it's lifetime.
