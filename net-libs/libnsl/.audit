This particular ebuild is a "fake" that installs nothing but depends on glibc *with* built-in libnsl. Needed for ebuilds that depend on glibc's nsl library.
Should be in 1.0-prime during it's lifetime.
